plugins {
    id 'java'
}

repositories {
    mavenCentral()
}

dependencies {
    implementation files("libs/poseidon.jar")
    // More potential implementations of dependencies if your plugin uses other plugins
}

jar {
    archiveBaseName = 'INSERT_NAME'
}